# CafeBoard

Дашборд для кофейни — управление заказами в реальном времени.

## Команда

| Агент | Роль | Директория |
|-------|------|------------|
| Oscar | Team Lead | координация |
| Kai | Backend | `backend/` |
| Cleo | Frontend | `frontend/` |
| Silas | Design | `docs/design/` |
| Aura | QA | `docs/qa/` |
| Arlo | DevOps | `backend/Dockerfile`, `frontend/Dockerfile`, `.github/`, `docker-compose.yml` |

## Правила

1. Каждый агент работает ТОЛЬКО в своей директории
2. Коммуникация через @-ники
3. Всегда на русском языке
4. Перед PR: `git diff --cached` → тесты → только потом PR
5. Никогда `git add -A` без проверки
